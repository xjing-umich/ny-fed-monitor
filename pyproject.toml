[project]
name = "sec-data-expansion"
version = "0.1.0"
description = "SEC-first fundamental data expansion pipeline"
requires-python = ">=3.9"
dependencies = [
  "requests>=2.31.0",
]

[project.scripts]
sec-data = "sec_data_pipeline.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
