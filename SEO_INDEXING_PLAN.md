# SEO 收录攻坚 Plan(handoff)

> 自包含文档。执行的 thread 不需要看原始对话即可照做。
> 创建于 2026-06-13。站点:thecompounder.fyi(Next.js 16 App Router,`/[lang]/` 双语 en/zh,ISR)。

## 背景与诊断(为什么做这件事)

- Vercel analytics 仅 ~10 访客/天。
- Google `site:thecompounder.fyi` = **0 条收录**。
- 技术地基**全部正常**:robots.txt 放行全站、sitemap.xml 合法(~445 URL = ~390 股票页 + 37 投资人页 + 内容页)、`meta robots = index,follow`、canonical 干净、有 hreflang。**不是技术 bug。**
- GSC「网页索引编制」状态(截至 2026-06-05):
  - 已编入索引 **0**
  - 未编入索引 24,其中:
    - **「已抓取-尚未编入索引」23 页** ← 核心问题
    - 「重复网页,Google 选择的规范网页与用户指定的不同」1 页
  - Google 目前只"认识" 24 / 445 页 → sitemap 尚未被完整处理。

**结论:** "已抓取-尚未编入索引" = Google 爬过但**主动决定先不收**。新站上这状态的两大主因:
- **A. 域名零权威/零外链** → Google 不信任,不愿花索引预算。
- **B. 445 页程序化生成、彼此太像、每页正文极薄(投资人页仅 1 句自动摘要 + 1 张持仓表)** → 被判"低附加值模板页"。

**反复点"请求编入索引"对这个状态基本无效**——它不是没爬到,是不想收。要改变判断,必须给信任信号(外链)+ 提升每页独特内容价值。

## 范围

本 plan 只覆盖**代码/内容我们能自己做的部分**。GSC 账号操作(提交 sitemap、请求收录、加外链到 X bio)由站主本人在另一处完成,不在本 plan 内,但任务 5 会产出可直接发布的稿子。

---

## 任务 1(最高杠杆):加深每个投资人页的服务端唯一正文

**问题:** 现在投资人页服务端可见正文只有一段自动生成句(例:"This quarter the investor added to Alphabet Inc and New York Times Co...")+ 持仓表。37 个页面结构雷同、文字极薄。

**目标:** 让每个投资人页有 **3~5 段、基于真实 13F 数据生成、彼此独一无二**的服务端可见正文(SSR,不是客户端 JS),给 Google 足够"这页值得收"的信号。注意保持现有合规纪律:**纯教育/数据描述,禁止买卖建议/目标价/推荐**(Investment Advisers Act)。

**相关文件:**
- `web/src/app/[lang]/investors/[slug]/page.tsx` — 投资人页(较大,先通读)
- `web/src/components/entity/EntityPage.tsx` — 已有可选 `aiNarrative?: React.ReactNode` 服务端节点 prop(在 `④ Data body` 上方渲染),以及 `notice` / `disclaimer` 槽位
- 现有那句自动摘要的**生成逻辑**需先定位(grep `This quarter the investor` / `added to` / narrative builder,可能在 `web/src/lib/` 下)。新正文应复用同一份已解析的 13F 数据,不要二次拉数。

**建议正文结构(每段都从真实数据派生,双语 en/zh):**
1. **这位投资人是谁** — 基金名、管理人、风格(集中/分散、换手高低,可由持仓数与集中度算出)。
2. **本季持仓全景** — 持仓数、前 N 大仓位占比、最大单一仓位百分比(数据已在页面表里)。
3. **本季动作** — 新建/加仓/减仓/清仓的具体标的(已有摘要可扩写成段)。
4. **长期持有的核心仓** — 连续持有 N 季的标的(页面已有 "held N quarters running" 信号)。
5. **数据出处与时点** — "Data as of 2026-03-31, filed 2026-05-15. Source: SEC EDGAR."(已有,保留)。

**硬约束:**
- 必须 **SSR**(`curl` 拿得到文字),不能靠客户端 hydration。
- 每页文字必须**因数据不同而不同**——避免"把同一模板套 37 遍"被判 doorway。用真实数字驱动措辞。
- **去 AI 感**(项目既定纪律):不用"not X, but Y"对仗、不用装饰性成对破折号、不堆三连排比、标题用冒号不用破折号。
- 保留现有 AI 免责声明那句。
- 复用 common 组件(`ProseDoc` / `DataTable` 等),遵循品牌 token 体系。

**验证:**
- `curl -s https://<dev>/en/investors/berkshire-hathaway | python3 -c "..."` 数出可见正文词数,目标从现在 ~2-3 句提升到 150~300 词唯一文字。
- 抽查 3 个不同投资人页,确认正文**内容真的不同**且数字与表格一致(数据准确性纪律:标源标日期)。
- `tsc` 通过(项目无测试套件,验证靠 tsc + 人工看页面)。

---

## 任务 2(次高杠杆):股票页同等加深 + 唯一化

**问题:** ~390 股票页是最容易被判"薄模板页"的一批(且要跟 Yahoo/MarketBeat 抢)。每页核心应回答 "**谁(哪些超级投资人)持有这只票、本季怎么动的**"——这是你相对 Yahoo 的差异化。

**相关文件:** `web/src/app/[lang]/stocks/[ticker]/page.tsx`(已做过 SEO 标题前置:`${issuer} (${ticker}) Stock — Who's Holding | Compounder`)。

**目标:** 每股票页加一段 SSR 唯一正文:有几位 superinvestor 持有本票、合计持仓、本季净增/净减、最大持有者是谁。全部从已有 13F 数据派生。**同样禁推荐/目标价。**

**优先级低于任务 1**:投资人页是差异化护城河、赢面高;股票页红海,先把投资人页吃透。可只先对**持有者≥3 的股票页**做(薄持有页不值得优先)。

---

## 任务 3:强化内链,帮 Google 把 24 页发现扩到 445 页

- 确认投资人页 ↔ 其持仓股票页**双向互链**(投资人页表格每行链到 `/stocks/<ticker>`;股票页"谁在持有"链回 `/investors/<slug>`)。
- 确认 `/en/investors` 和 `/en/stocks` 索引页**列出全部**条目且都是真链接(爬虫入口)。
- Learn 文章里出现的投资人/股票名,链到对应实体页(文章是已有内容,顺手加内链传权重)。
- 目的:稠密内链让 Googlebot 从少数已知页顺藤摸瓜爬全站,缓解"只认识 24 页"。

---

## 任务 4:修掉那 1 个 "重复网页,规范网页不同"

- 去 GSC 那一行点开拿到具体 URL(站主提供),或自查:常见根因是 en/zh 互判重复、带不带尾斜杠、或某页 canonical 指向了别页。
- 在对应 page 的 `generateMetadata` 里确认 `alternates.canonical` 指向自身规范 URL,且 hreflang 的 en/zh 互指正确。
- 1 页而已,优先级低,但顺手修。

---

## 任务 5:产出外链弹药(攻"域名零权威",站主去发布)

代码改不了 Google 对域名的信任——**外链才行**。本任务产出**可直接发布的稿子**,由站主本人发(不要程序化群发/纯链接,会被当 spam):

1. **一条 Reddit 真实数据帖**(r/ValueInvesting 或 r/SecurityAnalysis):用站里真实 Q1 2026 13F 数据讲**一个有洞见的发现**(例:跨基金共识买入了哪只票、某大佬罕见集中度),正文有干货,链接自然带出(不是纯链接帖——站主上次纯链接帖被删过)。
2. **X bio / 一条 build-in-public 推**:放站链接(X 链接会被 Google 爬,等于给新域名一个高权重入口)。
3. (可选)1~2 个 finance 工具目录提交文案。

**合规护栏:** 全部教育/数据口吻,禁荐股/目标价。诚实口吻(站主自己做的免费工具)。

---

## 执行顺序

1. **任务 1**(投资人页加深正文)— 结构性、杠杆最高,先做。
2. **任务 5**(外链稿)— 与任务 1 并行,站主可立即发布,直接攻信任。
3. **任务 3**(内链)— 跟任务 1 一起改,顺手。
4. **任务 2**(股票页)— 任务 1 验证有效后再铺开到 390 页。
5. **任务 4**(canonical)— 拿到 URL 后随手修。

## 完成后(站主侧,非本 plan)

- GSC「站点地图」提交 `sitemap.xml`(若还没成功)。
- 等 1~2 周回看 GSC「网页索引编制」:"已抓取-尚未编入索引"数字应开始下降、"已编入索引"从 0 往上走。
- 若仍卡 0,说明外链权重不够,继续累积外链,别靠反复"请求收录"。

## 全局纪律(项目既定,务必遵守)

- **数据准确性**:每处数字标来源(SEC EDGAR)和时点(as of 2026-03-31, filed 2026-05-15);不用过时数据。
- **合规**:纯教育/数据/方法论,禁买卖建议、目标价、个性化投资建议。
- **去 AI 感**:见任务 1 硬约束。
- **无测试套件**:验证用 `tsc`(先 `export PATH="/Users/junlinzhu/.nvm/versions/node/v20.20.0/bin:$PATH"`)+ curl 看 SSR + 人工看页面。
- **英文优先** SEO(中文为次语言仍收录)。
- 改完按项目惯例**提 PR**(base 分支按当前 git 状态确认,通常 `db-foundation`),不要直接 push 主干。
